<h1>Links</h1>

<ul>
  <% @links.each do |link| %>
    <li><%= link_to "#{link.address}", link_path(link) %>
    </li>
  <% end %>
</ul>


<%= link_to "New Link", new_link_path, :class => "btn btn-success" %>
